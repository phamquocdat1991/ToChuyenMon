import { sqliteTable, text, integer, index, uniqueIndex, type AnySQLiteColumn } from 'drizzle-orm/sqlite-core';
export const workspaces=sqliteTable('workspaces',{id:text('id').primaryKey(),owner:text('owner').notNull(),name:text('name').notNull(),year:text('year').notNull(),school:text('school').notNull()},t=>[uniqueIndex('workspace_owner').on(t.owner)]);
export const memberships=sqliteTable('memberships',{id:text('id').primaryKey(),workspace:text('workspace').notNull().references(()=>workspaces.id),email:text('email').notNull(),role:text('role').notNull(),member:text('member')},t=>[uniqueIndex('membership_email').on(t.email),index('membership_workspace').on(t.workspace)]);
export const records=sqliteTable('records',{id:text('id').primaryKey(),workspace:text('workspace').notNull().references(()=>workspaces.id),kind:text('kind').notNull(),year:text('year').notNull(),title:text('title').notNull(),status:text('status').notNull(),due:text('due').notNull().default(''),assignee:text('assignee').references(():AnySQLiteColumn=>records.id),parent:text('parent').references(():AnySQLiteColumn=>records.id),category:text('category').notNull().default(''),body:text('body').notNull().default(''),details:text('details').notNull().default('{}'),revision:integer('revision').notNull().default(1),updated:text('updated').notNull(),actor:text('actor').notNull()},t=>[index('records_workspace_kind').on(t.workspace,t.kind),index('records_parent').on(t.parent)]);
export const versions=sqliteTable('versions',{id:text('id').primaryKey(),record:text('record').notNull().references(()=>records.id),revision:integer('revision').notNull(),snapshot:text('snapshot').notNull(),actor:text('actor').notNull(),created:text('created').notNull(),note:text('note').notNull()},t=>[uniqueIndex('record_revision').on(t.record,t.revision)]);
export const files=sqliteTable('files',{id:text('id').primaryKey(),workspace:text('workspace').notNull().references(()=>workspaces.id),record:text('record').notNull().references(()=>records.id),name:text('name').notNull(),mime:text('mime').notNull(),size:integer('size').notNull(),created:text('created').notNull()},t=>[index('files_record').on(t.record)]);
